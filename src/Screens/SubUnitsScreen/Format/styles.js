import { StyleSheet } from "react-native";
import theme from "../../../../ColorCommon";
const styles = StyleSheet.create({
  containerContent: {
    flex: 1
  },
  duration: { height: 90, justifyContent: "center", padding: 16 }
});
export default styles;
